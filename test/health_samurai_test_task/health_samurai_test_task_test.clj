(ns health-samurai-test-task.health-samurai-test-task-test
  (:require [clojure.test :refer :all]
            [health-samurai-test-task.health-samurai-test-task :refer :all]))

(deftest a-test
  (testing "FIXME, I fail."
    (is (= 0 1))))
